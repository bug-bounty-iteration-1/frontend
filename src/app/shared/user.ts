export class User {
    constructor(
        userId: number,
        firstName: String,
        lastName: String,
        userName: String,
        password: String,
       // currentRole: Role
    ) {

    }
}